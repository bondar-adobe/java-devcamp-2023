package com.adobe.devcamp.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class CampaignController {
    // TODO:    add endpoint for creating a new campaign
    //          add endpoint for retrieving a campaign by id - GET/campaignes/{id}
    //          add endpoint for retrieving all users part of a campaign GET/campaignes/{id}/users
}
